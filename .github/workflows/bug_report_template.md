**Describe the bug**
Brief description of the issue.  

**Context - Include these if possible**
1. Get the current software and data version:  
`rolypoly version`   
2. Try to rerun the command from with the debug logging turned on:
`rolypoly <command> <options> --log-level debug --log-file <somewhere you know>`  
3. Potentially, if appropiate, use the `--keep-tmp` and `--temp-dir` flags.  
4. Include the complete log file and the command used.   
5. Include the method you installed the software (pip/conda/pixi).  
6. Include system/hardware specs (Linux xyz, nnn CPU...), and if the command was called with a job manager (SLURM/PBS). 
